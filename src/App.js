import Contador from './Contador';
const App = () => {

    return (
        <>
        <Contador valor={0} numero={1}/>
        <Contador valor={100} numero={2}/>
        </>
    );
}

export default App;
